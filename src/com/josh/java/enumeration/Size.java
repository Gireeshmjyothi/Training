package com.josh.java.enumeration;

//Here, we have created an enum named size and it contains fixed values
public enum Size {
	// These values are called enum constants
	SMALL, MEDIUM, LARGE, EXTRALARGE;
}
